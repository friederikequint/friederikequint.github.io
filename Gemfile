# Gemfile
source "https://rubygems.org"

gem "github-pages", "~> 232", group: :jekyll_plugins

# Local dev only
group :development do
  gem "webrick", "~> 1.8"
end

# Optional: Windows-only helper for local dev
gem "wdm", "~> 0.1.0", platforms: [:mingw, :x64_mingw, :mswin]
